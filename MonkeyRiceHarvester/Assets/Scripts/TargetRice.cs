using System;
using UnityEngine;

public class TargetRice : MonoBehaviour
{
    // [Header("Rice Bag Stats")]
    // public long currentLevel = 1;
    // public double maxHP;
    // public double currentHP;
    // public double firstHP;

    // public double riceReward;

    // public GameManager gameManager;
    // public static event Action<double, double> OnHPChanged;
    // // 쌀 포대 체력이 변경될 때 UIManager에 알리는 이벤트 (currentHP, maxHP)

    // void Start()
    // {
    //     gameManager = GameManager.instance;
        
    // }

    // public void Init(long level) // 레벨에 따라 스탯을 계산 및 초기화
    // {
    //     maxHP = firstHP * Math.Pow(1.15, level - 1);
    //     currentHP = maxHP;

    //     riceReward = 50 * level;

    //     currentLevel = level;

    //     OnHPChanged?.Invoke(currentHP, maxHP);


    // }


}
